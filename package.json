{
  "name": "uc-portfolio",
  "version": "2.0.0",
  "description": "UC-Portfolio Momentum Dashboard & Analysis API",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute uc-portfolio-db --file=./schema.sql && wrangler d1 execute uc-portfolio-db --file=./seed.sql"
  }
}
